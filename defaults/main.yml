---

# locations
dvm_dir: "/usr/lib/dvm"
dvm_location: "{{ dvm_dir }}/dvm"

# version
dvm_drush_version: 8.0.0-rc3

# install packages?
dvm_install_packages: true

# packages to install
dvm_packages:
  - acquia_purge
  - delete_all
  - drush_extras
  - drush_sql_extras
  - registry_rebuild
